import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class OmuzPage extends StatelessWidget {
  const OmuzPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Omuz Page'),
      ),
    );
  }
}