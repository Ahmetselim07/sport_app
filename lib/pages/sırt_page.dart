import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class SirtPage extends StatelessWidget {
  const SirtPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sırt Page'),
      ),
    );
  }
}